# R Dependency Classification

This document records dependencies for both `dorado_workflow/data/NanoTel.R` and the shipped scripts
under `dorado_workflow/r_analysis/`. It separates direct runtime requirements from packages supplied
by R, transitive dependencies, packages awaiting team classification, and representative execution
tests that have not yet been supplied.

## NanoTel.R

### Accepted Direct Dependencies

| R library | Conda package | Process supported |
| --- | --- | --- |
| `optparse` | `r-optparse` | Parse NanoTel command-line arguments. |
| `BiocGenerics` | `bioconductor-biocgenerics` | Supply generic operations used by Bioconductor sequence and range objects. |
| `S4Vectors` | `bioconductor-s4vectors` | Supply the vector infrastructure underlying the Bioconductor objects. |
| `IRanges` | `bioconductor-iranges` | Represent, combine, and measure telomeric-pattern coordinates. |
| `Biostrings` | `bioconductor-biostrings` | Read FASTA/FASTQ, reverse-complement reads, and match DNA patterns. |
| `conflicted` | `r-conflicted` | Make ambiguous function selection explicit under the current imports. |
| `dplyr` | `r-dplyr` | Filter, sort, append, and mutate result tables. |
| `readr` | `r-readr` | Write CSV and text outputs. |
| `purrr` | `r-purrr` | Build the current telomere-pattern parser with `compose()` and `partial()`. |
| `stringr` | `r-stringr` | Inspect, split, and manipulate pattern strings. |
| `ggplot2` | `r-ggplot2` | Produce read-level and post-processing plots. |
| `future` | `r-future` | Select multicore or multisession execution plans. |
| `logr` | `r-logr` | Create and write the NanoTel run log. |
| `ggprism` | `r-ggprism` | Style the plot generated by the approved optional `--analysis` process. |
| `testit` | `r-testit` | Enforce pattern/subsequence assumptions with `assert()`. |
| `survival` | `r-survival` | Future-proof the disabled KM metrics implementation using `Surv()` and `survfit()`. |

`r-survival` is installed now even though `include_km_metrics` is currently `FALSE`. This avoids a
future feature activation that forgets its runtime dependency. Its presence does not approve the KM
statistics, calibration model, or excluded `.rds` resource.

### Supplied Or Transitive

- `parallel` is required by NanoTel but is supplied with R, so the recipe does not list a separate
  Conda package.
- `tibble` is installed transitively by accepted packages such as `dplyr` and `readr`. NanoTel has no
  confirmed direct use that requires a separate recipe entry.
- The `%>%` operator is available through the accepted tidyverse component dependencies; no separate
  direct `magrittr` declaration is currently needed.

### Not Direct NanoTel Dependencies For Now

- `tidyverse` is an umbrella package and is intentionally not declared directly. NanoTel's actual
  component dependencies are listed individually.
- `tidyr`, `forcats`, and `lubridate` have no confirmed active NanoTel calls. Do not interpret this as
  removal from the entire project: `r-tidyr` is still required by the separate core R-analysis code.

No change to `NanoTel.R` has been approved to remove or replace its current `require(tidyverse)` and
fallback package-loading block. The dependency decision and source cleanup decision remain separate.

## r_analysis Runtime Scripts

The shared `load_required_packages()` function currently attempts to install and load thirteen CRAN
packages for every analysis run. `main_analysis_pipeline.R` also loads `jsonlite`. The Bioconda draft
lists all fourteen packages so that an installed workflow does not download packages at runtime.

### Team-Approved Core Dependencies

| R library | Conda package | Core process |
| --- | --- | --- |
| `dplyr` | `r-dplyr` | Filter, join, group, and transform mapping, NanoTel-summary, and methylation tables. |
| `readr` | `r-readr` | Read input CSV files and write analysis CSV outputs. |
| `stringr` | `r-stringr` | Extract and normalize barcode names from filenames and directories. |
| `jsonlite` | `r-jsonlite` | Read workflow configuration and write temporary subprocess configuration. |
| `ggplot2` | `r-ggplot2` | Static methylation plots in `functions/methylation_functions.R`. |
| `plotly` | `r-plotly` | Optional interactive methylation HTML and generated Shiny plots. |
| `shiny` | `r-shiny` | Optional generated methylation viewer application. |
| `htmlwidgets` | `r-htmlwidgets` | Save optional interactive Plotly output as HTML. |
| `RColorBrewer` | `r-rcolorbrewer` | Select methylation plot and generated Shiny-app color palettes. |

The visualization packages are accepted because the shipped default configuration enables static
plots, original-style interactive HTML, and Shiny-app generation. The pipeline generates the Shiny
application files but does not launch a Shiny server automatically.

### Loaded But No Active Call Found

| R library | Conda package | Current evidence |
| --- | --- | --- |
| `tidyr` | `r-tidyr` | Listed by `load_required_packages()`; no active function call found in a shipped runtime R script. |
| `purrr` | `r-purrr` | Listed by `load_required_packages()`; no active function call found in a shipped runtime R script. NanoTel uses it separately. |
| `gridExtra` | `r-gridextra` | Listed by `load_required_packages()`; no active function call found. |
| `fuzzyjoin` | `r-fuzzyjoin` | Listed by `load_required_packages()`; no active function call found. |
| `readxl` | `r-readxl` | Listed by `load_required_packages()`; no active function call found. |

Do not remove these packages from the recipe or `utils.R` until the team reviews the processes and
representative R-analysis inputs exercise the relevant workflow paths.

### Runtime Installation Concern

`functions/utils.R` currently calls `install.packages()` when a listed package is missing. A released
Conda package should normally use only dependencies installed by Conda and should not modify its R
library or require internet access while the workflow is running. This behavior needs a separate,
reviewed code decision after the dependency list is confirmed.

## PACKAGING TODO: Complete Representative Execution Tests

Sanitized team-supplied FASTQ and BAM fixtures and a gated integration test now cover normal NanoTel
`--summary_only`, NanoTel filtration, aligned-BAM summary generation, mapping, BAM filtering, and
the absence of methylation tags. On 2026-08-05, all three tests passed in an isolated Linux
Micromamba environment containing the dependency set declared by the draft recipe. Approved
scientific regression values are recorded in `tests/fixtures/integration/expected_results.json`.

The remaining release validation is to run the same test against the package produced by the actual
Bioconda/conda-build recipe rather than directly against the source checkout.

Additional test coverage is still needed for:

1. NanoTel processing with `--summary_only --analysis`.
2. Reverse complement, filtering, and TVR patterns when the
   supplied fixtures support those paths.
3. A BAM containing approved modified-base tags for actual methylation analysis.

Verify successful exit, expected summary/read-ID outputs, expected `--analysis` outputs, and absence
of missing-function or missing-package failures. The clean test environment must omit the direct
`r-tidyverse`, `r-forcats`, and `r-lubridate` packages. `tibble` may still be installed transitively.

Future fixtures should also cover optional static plots, optional interactive HTML, and an optional
generated Shiny app according to the features the team classifies as supported.
